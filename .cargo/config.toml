# Cargo 配置 - 混合静态/动态链接策略

[target.x86_64-unknown-linux-musl]
# 部分静态链接：能静态的就静态，不能的就动态
# -C target-feature=-crt-static 禁用完全静态链接
rustflags = ["-C", "target-feature=-crt-static"]

[build]
# 默认目标
target = "x86_64-unknown-linux-musl"
